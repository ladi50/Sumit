import "./Spinner.css";

const Spinner = () => {
    return (
        <div className={"spinnerWrapper"}>
            <div className="loader">Loading...</div>
        </div>
    );
};

export default Spinner;